import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const TabButtons: FC<Props> = (props) => {
	return (
		<View>
			<Text>TabButtons</Text>
		</View>
	)
}

export default TabButtons
