import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const RoundedButtons: FC<Props> = (props) => {
	return (
		<View>
			<Text>RoundedButtons</Text>
		</View>
	)
}

export default RoundedButtons
