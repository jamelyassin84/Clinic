import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const TimeBadges: FC<Props> = (props) => {
	return (
		<View>
			<Text>TimeBadges</Text>
		</View>
	)
}

export default TimeBadges
