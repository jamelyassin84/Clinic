import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const VerificationForm: FC<Props> = (props) => {
	return (
		<View>
			<Text>VerificationForm</Text>
		</View>
	)
}

export default VerificationForm
