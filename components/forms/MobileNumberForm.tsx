import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const MobileNumberForm: FC<Props> = (props) => {
	return (
		<View>
			<Text>MobileNumberForm</Text>
		</View>
	)
}

export default MobileNumberForm
