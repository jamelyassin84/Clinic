import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const EnterMobileNumberForm: FC<Props> = (props) => {
	return (
		<View>
			<Text>EnterMobileNumberForm</Text>
		</View>
	)
}

export default EnterMobileNumberForm
