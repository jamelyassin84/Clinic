import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const _VerificationCode: FC<Props> = (props) => {
	return (
		<View>
			<Text>_VerificationCode</Text>
		</View>
	)
}

export default _VerificationCode
