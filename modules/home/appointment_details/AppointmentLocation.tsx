import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const AppointmentLocation: FC<Props> = (props) => {
	return (
		<View>
			<Text>AppointmentLocation</Text>
		</View>
	)
}

export default AppointmentLocation
