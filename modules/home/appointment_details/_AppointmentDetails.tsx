import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const _AppointmentDetails: FC<Props> = (props) => {
	return (
		<View>
			<Text>_AppointmentDetails</Text>
		</View>
	)
}

export default _AppointmentDetails
