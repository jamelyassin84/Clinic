import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const AppointmentDoctor: FC<Props> = (props) => {
	return (
		<View>
			<Text>AppointmentDoctor</Text>
		</View>
	)
}

export default AppointmentDoctor
