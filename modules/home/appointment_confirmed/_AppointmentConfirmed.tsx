import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const _AppointmentConfirmed: FC<Props> = (props) => {
	return (
		<View>
			<Text>_AppointmentConfirmed</Text>
		</View>
	)
}

export default _AppointmentConfirmed
