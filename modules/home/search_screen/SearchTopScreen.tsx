import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const SearchTopScreen: FC<Props> = (props) => {
	return (
		<View>
			<Text>SearchTopScreen</Text>
		</View>
	)
}

export default SearchTopScreen
