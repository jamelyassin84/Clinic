import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const PopularSearches: FC<Props> = (props) => {
	return (
		<View>
			<Text>PopularSearches</Text>
		</View>
	)
}

export default PopularSearches
