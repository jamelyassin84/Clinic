import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const ProfileAndLocation: FC<Props> = (props) => {
	return (
		<View>
			<Text>ProfileAndLocation</Text>
		</View>
	)
}

export default ProfileAndLocation
