import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const AppointMentTab: FC<Props> = (props) => {
	return (
		<View>
			<Text>AppointMentTab</Text>
		</View>
	)
}

export default AppointMentTab
