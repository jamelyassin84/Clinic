import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const MiddleMap: FC<Props> = (props) => {
	return (
		<View>
			<Text>MiddleMap</Text>
		</View>
	)
}

export default MiddleMap
