import React, { FC } from 'react'
import { Text, View } from 'react-native'

type Props = {}

const TopProfile: FC<Props> = (props) => {
	return (
		<View>
			<Text>TopProfile</Text>
		</View>
	)
}

export default TopProfile
